export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other'
}

export enum Role {
  ADMIN = 'admin',
  ORGANIZER = 'organizer',
  CUSTOMER = 'customer'
}

export enum Status {
  ACTIVE = 1,
  INACTIVE = 0
}
